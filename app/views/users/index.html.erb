<% provide(:title, 'ユーザー一覧') %>
<div id="fh5co-portfolio">
  <div class="container">
    <%= will_paginate %>
    <ul class="users">
      <%= render @users %>
    </ul>
    <%= will_paginate %>
  </div>
</div>